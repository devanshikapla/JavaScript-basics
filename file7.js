let k="                 Jaypee institute of information technology  "
let j=k.length;
console.log(j);

// trim method :- use to remove spaces as strings are immutable so it will give new string

let newstring=k.trim();
console.log(newstring);
console.log(newstring.length);

//to uppercase

let x="Devanshi";
console.log(x.toUpperCase());
let y="Kapla";
console.log(y.toLowerCase());
//slice method returns the string between specific range and upperlimit is not included
let newString=x.slice(0,5);
console.log(newString);
